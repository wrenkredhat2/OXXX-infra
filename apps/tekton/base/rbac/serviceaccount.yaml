# Managed Manifest
# File: platform/tekton/base/rbac/serviceaccount.yaml
# Purpose: RBAC policy granting minimum required permissions to Tekton service accounts and triggers.
# Owner: integration-hub platform team

apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-ci-bot
secrets:
  - name: registry-auth-secret
  - name: gitops-push-secret
  - name: maven-repo-credentials
